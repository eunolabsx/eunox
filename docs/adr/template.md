# ADR-NNNN: <short imperative title>

- **Status:** Draft
- **Date:** YYYY-MM-DD
- **Deciders:** <who made the call — finalizing is by maintainer consensus>

## Context

What forces the decision? Describe the problem, the constraints, and the
relevant invariants (link to [CONTRIBUTING.md](../../CONTRIBUTING.md),
[architecture.md](../architecture.md), or [the threat
model](../threat-model-mcp.md) where they apply). State the facts that were
true when this was written — enough that a reader who wasn't there can judge
the decision on its merits.

## Decision

The choice, stated plainly in active voice: "We will ...". Point at the code
that implements it (`file.go:line`) if it already exists.

## Alternatives considered

The credible options that were rejected, each with a one-line reason. If there
was no real alternative, this decision probably doesn't need an ADR.

- **<Alternative A>** — why not.
- **<Alternative B>** — why not.

## Consequences

What this commits us to — the good and the bad. What becomes easy, what becomes
harder, what new obligations the codebase now carries (tests to keep green,
docs to keep in sync, failure modes to watch). Be honest about the costs.
