# Go build artifacts
coverage.out
*.out
*.test
*.exe
*.exe~
*.dll
*.so
*.dylib
go.work
go.work.sum

# Binary output
/bin/

# goreleaser / .mcpb packaging output
/dist/
/dist-mcpb/

# MCP binary (not the eunox/ folder)
/eunox
!/eunox/

# Demo e2e binaries produced by go build
/mock-host
/mock-server

# Vulnerability scan output (scripts/govulncheck.sh --json-out/--summary-out; the
# workflows write these at the repo root)
/govulncheck.json
/summary.md

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Demo audit log and HMAC key (written by eunox container; keep the directory, not the data)
demo/audit/*.jsonl
demo/audit/*.jsonl.*
demo/audit/*.key

# Trifecta persistent-audit tape (written by `make -C demo trifecta-audit`)
demo/trifecta/audit/

# Temporary files
tmp/
temp/
*.tmp

# Cloudflare Wrangler build output
.wrangler/

# Site deploy output (assembled by .github/workflows/deploy-site.yml)
site/dist/
